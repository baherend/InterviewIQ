# Interview IQ — NLP Module
# Pinned dependency manifest (Phase 2).
#
# CPU-First dev install:
#   pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Kaggle GPU install (Thin Runner notebooks):
#   pip install -r requirements.txt          # torch pre-installed on T4 image

# ── Core ML ────────────────────────────────────────────────────────────────────
torch==2.2.2
transformers==4.40.2
peft==0.10.0
datasets==2.18.0
accelerate==0.29.3

# ── NLI model (mDeBERTa-v3-base-mnli-xnli) — no extra package needed;
#    loaded via transformers.AutoModelForSequenceClassification

# ── Embeddings (BGE-M3) ────────────────────────────────────────────────────────
FlagEmbedding==1.2.5
sentence-transformers==2.7.0

# ── ASR (faster-whisper wraps CTranslate2 + ctranslate2 backend) ──────────────
faster-whisper==1.1.1

# ── VAD ────────────────────────────────────────────────────────────────────────
# Silero VAD is loaded via torch.hub at runtime (no separate PyPI package
# required); torchaudio provides waveform I/O utilities.
torchaudio==2.2.2

# ── Configuration ──────────────────────────────────────────────────────────────
PyYAML==6.0.1

# ── Numerics / data handling ───────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
scipy==1.13.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0

# ── LLM API client (D74 decomposition_llm) ──────────────────────────────────────
requests==2.34.2
python-dotenv==1.0.1
